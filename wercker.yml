box: wercker/ruby
services:
  - dtan4/kuromoji-elasticsearch@0.0.11
build:
  steps:
    - script:
        name: curl
        code: |
          sudo apt-get update
          sudo apt-get install -y curl
          echo $WERCKER_ELASTICSEARCH_HOST
          ping -c 5 $WERCKER_ELASTICSEARCH_HOST
          curl http://$WERCKER_ELASTICSEARCH_HOST:9200
    # - script:
    #     name: Check docker version
    #     code: |
    #       docker -v
    # - script:
    #     name: Run test
    #     code: >
    #       script/test
